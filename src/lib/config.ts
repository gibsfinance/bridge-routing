import { PUBLIC_IMAGE_ROOT, PUBLIC_INDEXER_URL, PUBLIC_WALLET_CONNECT_ID } from '$env/static/public'

export const imageRoot = PUBLIC_IMAGE_ROOT || 'https://gib.show'

export const indexer = PUBLIC_INDEXER_URL || 'https://pulsechain-bridge-indexer.up.railway.app'

export const walletConnectProjectId = PUBLIC_WALLET_CONNECT_ID
