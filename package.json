{
  "name": "onboarding",
  "version": "1.10.0",
  "private": true,
  "access": "private",
  "workspaces": [
    "packages/common",
    "packages/bridge-sdk",
    "packages/indexer",
    "packages/ui"
  ],
  "scripts": {
    "docker:build": "docker compose build",
    "version": "node scripts/sync-versions.js",
    "ui:dev": "yarn workspace ui run dev",
    "ui:preview": "yarn workspace ui run preview",
    "ui:build": "yarn workspace ui run build",
    "common:build": "yarn workspace @gibs/common run build",
    "ui:check": "yarn workspace ui run check",
    "ui:codegen": "yarn workspace ui run codegen",
    "bridge-sdk:build": "yarn workspace @gibs/bridge-sdk run build",
    "build": "yarn run common:build && yarn run bridge-sdk:build && yarn run indexer:build && yarn run ui:build",
    "indexer:build": "yarn workspace indexer run build",
    "indexer:dev": "yarn workspace indexer run dev",
    "dev": "yarn run ui:dev",
    "preview": "yarn run ui:preview",
    "pinata:publish": "cd packages/ui && pinata-cli -a $PINATA_API_KEY && pinata-cli -u dist",
    "check": "yarn run common:build && yarn run bridge-sdk:build && yarn run ui:check",
    "clean": "rm -rf node_modules && rm -rf packages/*/node_modules && rm -rf packages/*/dist",
    "packages:publish": "cd packages/common && npm publish && cd ../bridge-sdk && npm publish"
  },
  "keywords": [],
  "resolutions": {
    "esbuild": "0.25.0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "pinata-upload-cli": "^1.1.0",
    "typescript": "~5.7.2"
  },
  "packageManager": "yarn@4.10.3",
  "dependencies": {
    "esbuild": "0.25.10"
  }
}
